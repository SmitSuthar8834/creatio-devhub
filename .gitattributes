# Shell scripts must stay LF even on a Windows checkout, or the shebang breaks
# on macOS/Linux ("bash\r: No such file or directory").
*.sh text eol=lf

# Keep CI/config that Unix runners read on LF too.
*.yml text eol=lf
*.yaml text eol=lf
